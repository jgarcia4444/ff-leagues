import React from 'react'

const SectionTitle = ({text}) => {

    return (
        <div className="">
            <h3 className="text-3xl text-bold">{text}</h3>
        </div>
    )
};

export default SectionTitle;